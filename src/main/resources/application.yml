spring:
  datasource:
    url: jdbc:mysql://localhost:3306/graduation_project

    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update  # You can use 'create', 'validate', or 'none' depending on your use case
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
